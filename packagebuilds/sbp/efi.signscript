#!/bin/sh
# Maintained by sbp-efi
KEY=/etc/sbkeys/DB.key
CER=/etc/sbkeys/DB.crt
if [ -e $KEY ] && [ -e $CER ]
then
    /usr/bin/sbsign --key $KEY --cert $CER --output $1 $1
else
    echo "SB keys not found in /etc/sbkeys/DB.{key,crt}"
fi
