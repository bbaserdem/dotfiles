#----------------#
#-----CONFIG-----#
#----------------#

# Autostart programs
exec --no-startup-id "dex --autostart --environment sway"

# Fonts
font pango:sans-serif 11px

# Gaps
gaps inner 16
gaps outer 0

# Use to drag floating with mod
floating_modifier $Meta

# Hide title bars
hide_edge_borders both
default_border none

# Status bar;
#bar { swaybar_command waybar }
